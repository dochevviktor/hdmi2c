# Continuing this project

Read `PLAN.md` and `readme.md` before continuing the hardware/firmware work.
`PLAN.md` records the staged redesign, decisions, verification results, and next
action across sessions. Update its checkboxes and session handoff when work changes
the project status. Keep completed CAD checks distinct from physical hardware tests.

Inspect `git status` before editing and preserve existing user changes.
